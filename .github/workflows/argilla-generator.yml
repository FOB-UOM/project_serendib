name: Argilla Generator

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  argilla-generator:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Argilla task generation stub
        run: |
          echo "Argilla generation placeholder: wire shared/scripts pipeline here."
          python shared/scripts/gamification_cli.py --state shared/argilla/infra/state.json leaderboard --top 5 || true
