# Synthetic Generation (`synthetic-gen/`)

Scripts for generating *candidate* data (to be human-verified) using a **Bonito / LLaMA-Factory style** pipeline.

Principles:

- Synthetic data is **always tagged** as synthetic with provenance.
- Student verification is required before release.
- Prefer domain-grounded generation (seed documents, outlines, rubrics).

