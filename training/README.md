# Training (`training/`)

Training scripts for:

- **Instruction tuning** (Layer 1) using Unsloth QLoRA / PEFT
- **Continued pre-training** (Layer 0) on the raw corpus
- Mixed training strategies (v1+)

Entry points:

- `training/train_unsloth_qlora.py` — instruction fine-tune baseline (7B-class)

